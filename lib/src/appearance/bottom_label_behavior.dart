/// Label visibility behavior for bottom bar items.
enum BottomLabelBehavior {
  /// Always show all labels.
  alwaysShow,

  /// Show only the selected destination label.
  onlySelected,

  /// Never show labels.
  alwaysHide,
}
