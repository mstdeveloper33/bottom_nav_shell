name: bottom_shell_nav_example
description: Example app for bottom_shell_nav.
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  auto_route: ^11.1.0
  bottom_shell_nav:
    path: ..
  flutter:
    sdk: flutter
  go_router: ">=13.0.0 <18.0.0"

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
